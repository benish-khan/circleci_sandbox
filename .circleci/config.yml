version: 2.1
jobs:
  build:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run: echo 'hello world from the build step'

  accept_test_1:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD

    steps:
      - checkout
      - run: echo 'Hello from accept_test_1'

  accept_test_2:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD

    steps:
      - checkout
      - run: echo 'Hello from accept_test_2'

  accept_test_3:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD

    steps:
      - checkout
      - run: echo 'Hello from accept_test_3'

  accept_test_4:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD

    steps:
      - checkout
      - run: echo 'Hello from accept_test_4'

  deploy:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD

    steps:
      - run: echo 'This step is run only when all 4 accept_tests are successfully completed.'

workflows:
  version: 2
  build-accept-deploy:
    jobs:
      - build
      - accept_test_1:
          requires:
            - build 
      - accept_test_2:
          requires:
            - build
      - accept_test_3:
          requires:
            - build
      - accept_test_4:
          requires:
            - build
      - deploy:
          requires:
            - accept_test_1
            - accept_test_2
            - accept_test_3
            - accept_test_4