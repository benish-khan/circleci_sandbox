version: 2.1

jobs:
  test:
    machine: true
    resource_class: benish-khan/bebe-runner
    steps:
      - run: echo 'Hello World!'
      - run: echo 'Hello from BBz Runner!!!'

workflows:
  version: 2
  test_steps:
    jobs:
      - test
      


