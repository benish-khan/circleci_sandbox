version: 2.1
jobs:
  build:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run: echo 'hello world from the build step'

  test_1:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD

    steps:
      - checkout
      - run: echo 'Hello from test_1'

  test_2:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD

    steps:
      - checkout
      - run: echo 'Hello from test_2'

  deploy:
    docker:
      - image: 'circleci/python:latest'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD

    steps:
      - run: echo 'Final Deploy Step!'
workflows:
  version: 2
  build-test-and-approval-deploy:
    jobs:
      - build
      - test_1:
          requires:
            - build 
          context: org-global
      - test_2:
          requires:
            - test_1
          context: org-global     
      - deploy:
          requires:
            - test_2
